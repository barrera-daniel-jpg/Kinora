import { defineConfig } from 'vite';

export default defineConfig({
  // Aquí puedes agregar configuraciones si las necesitas más adelante, 
  // pero por ahora este objeto vacío es completamente válido.
});